// Everything on this site is static prose, so prerender the whole thing.
// adapter-static then emits plain HTML that GitHub Pages serves directly.
export const prerender = true;
export const trailingSlash = 'always';
